<!-- app/views/dreams_analytics/analytics.html.erb -->
<h2>Dream Analytics</h2>
<canvas id="dreamPieChart" width="400" height="400"></canvas>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
  // Use JavaScript to fetch analytics data and create a pie chart
  // Example data (replace with actual analytics data)
  var data = {
    labels: ['Nightmare', 'Pregnancy', 'Memory', 'Ex', 'Grandfather', 'Sleep paralysis', 'Sea', 'Animal'],
    datasets: [{
      data: [30, 20, 10, 40],
      backgroundColor: ['#FF6384', '#36A2EB', '#FFCE56', '#4CAF50'],
    }]
  };

  var ctx = document.getElementById('dreamPieChart').getContext('2d');
  var myPieChart = new Chart(ctx, {
    type: 'pie',
    data: data,
  });
</script>
